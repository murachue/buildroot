config BR2_PACKAGE_LIBDRAGON
	bool "libdragon"
	default n
	depends on BR2_mips || BR2_mips64
	help
	  The homebrew library for developing Nintendo 64.
	  It contains a library for developing N64 ROMs.

if BR2_PACKAGE_LIBDRAGON

comment "for specifying version, see \"Host utilities\" section."

endif
